@inject SharedLocalizationService sharedLocalizer
@inject IViewLocalizer localizer
@model Blog
<section class="blog-area section-gap">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="main_title">
                    <p class="top_text">@localizer["My blog"] <span></span></p>
                    <h2>
                        @localizer["Latest Stories From"] <br>
                        @localizer["My blog"]
                    </h2>
                </div>
            </div>
        </div>
        <div class="row">
            @foreach (var blogItem in Model.BlogItemsMini)
            {
                var link = Url.ActionLink("Post", "Blog", new { name = blogItem.Name });
                <div class="col-lg-4 col-md-6">
                    <div onclick="location.href='@link'" style="cursor: pointer;" class="single-blog">
                        <div class="thumb">
                            <img class="img-fluid blog-post-thumbnail" src="@blogItem.Image" alt="">
                        </div>
                        <div class="short_details">
                            <div class="meta-top d-flex">
                                <span><i class="ti-user"></i> @localizer[blogItem.Author] | <i class="ti-calendar"></i> @blogItem.Date.ToString("MMM dd, yyyy")</span>
                            </div>
                            <a class="d-block" href="@link">
                                <h4>@localizer[blogItem.Title]</h4>
                            </a>
                            <div class="text-wrap">
                                <p>
                                    @localizer[blogItem.Content]
                                </p>
                            </div>
                            <a href="@link" class="primary_btn2">@localizer["Learn More"]</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>